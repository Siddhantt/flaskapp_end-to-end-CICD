global:
  resolve_timeout: 5m

route:
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h
  receiver: 'slack-notifications'

receivers:
  - name: 'slack-notifications'
    slack_configs:
      - api_url: 'https://hooks.slack.com/services/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'
        channel: '#alerts'
        send_resolved: true
        title: "🔥 Alert: {{ .CommonAnnotations.summary }}"
        text: "{{ .CommonAnnotations.description }}"

